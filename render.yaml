services:
  - type: web
    name: tts-voice-studio
    env: python
    rootDir: voice_studio
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    plan: free
    autoDeploy: true
